# ── Dataset files (too large for Git) ──
ml-engine/data/*.csv
ml-engine/data/*.pcap
ml-engine/data/*.zip

# ── Trained model files (too large, regenerate from train.py) ──
ml-engine/models/*.pkl
ml-engine/models/*.joblib

# ── Runtime database ──
shared/alerts.db
shared/*.db

# ── Secrets ──
.env

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/
.idea/
*.swp
*.swo